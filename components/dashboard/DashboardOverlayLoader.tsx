"use client";

export default function DashboardOverlayLoader() {
  // Placeholder: later you can show a full-screen overlay when global dashboard
  // actions are running (e.g., saving, bulk operations, etc.).
  return null;
}